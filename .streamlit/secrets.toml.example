# Copy this file to .streamlit/secrets.toml (which is gitignored) and fill in
# real values. On Streamlit Community Cloud, paste the same keys into the
# app's "Secrets" settings panel instead of committing a secrets.toml file.

APP_PASSWORD = "change-me"
CURSOR_API_KEY = "your-cursor-api-key"
